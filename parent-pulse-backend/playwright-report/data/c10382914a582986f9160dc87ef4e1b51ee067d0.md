# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: grades.spec.js >> scrape grades
- Location: tests\grades.spec.js:5:1

# Error details

```
Error: page.goto: Target page, context or browser has been closed
```

# Test source

```ts
  1  | // @ts-check
  2  | import { test, expect } from '@playwright/test';
  3  | import fs from 'fs';
  4  | 
  5  | test('scrape grades', async ({ page }) => {
  6  |   // Load portal using saved session
> 7  |   await page.goto('https://sis.factsmgt.com/family-portal/');
     |              ^ Error: page.goto: Target page, context or browser has been closed
  8  |   await page.waitForLoadState('networkidle');
  9  | 
  10 |   // Detect iframe if present
  11 |   let frame = page.frame({ url: /family-portal/ });
  12 |   if (!frame) frame = page;
  13 | 
  14 |   // Optional modal
  15 |   const maybeLater = frame.locator('[data-test-id="maybe-later-button"]');
  16 |   if (await maybeLater.isVisible().catch(() => false)) {
  17 |     await maybeLater.click();
  18 |   }
  19 | 
  20 |   // STEP 1 — Click the student name (required to enter student dashboard)
  21 |   const studentCard = frame.locator('mat-card:has-text("Student")');
  22 |   await studentCard.first().waitFor({ state: 'visible', timeout: 15000 });
  23 |   await studentCard.first().click();
  24 | 
  25 |   await frame.waitForLoadState('networkidle');
  26 | 
  27 |   // STEP 2 — Open Student drawer
  28 |   const studentDrawer = frame.locator('mat-expansion-panel-header:has-text("Student")');
  29 |   await studentDrawer.waitFor({ state: 'visible', timeout: 15000 });
  30 |   await studentDrawer.click();
  31 | 
  32 |   // STEP 3 — Click Grades
  33 |   const gradesLink = frame.getByRole('link', { name: /grades/i });
  34 |   await gradesLink.waitFor({ state: 'visible', timeout: 15000 });
  35 |   await gradesLink.click();
  36 | 
  37 |   await frame.waitForLoadState('networkidle');
  38 | 
  39 |   // STEP 4 — Wait for table rows
  40 |   const rows = frame.locator('table tbody tr');
  41 |   await rows.first().waitFor({ timeout: 20000 });
  42 | 
  43 |   // STEP 5 — Scrape rows
  44 |   const grades = await frame.$$eval('table tbody tr', rows =>
  45 |     rows.map(row => {
  46 |       const cells = row.querySelectorAll('td');
  47 |       return {
  48 |         course: cells[0]?.textContent?.trim() ?? '',
  49 |         grade: cells[1]?.textContent?.trim() ?? '',
  50 |         percentage: cells[2]?.textContent?.trim() ?? '',
  51 |       };
  52 |     })
  53 |   );
  54 | 
  55 |   console.log('Scraped rows:', grades.length);
  56 | 
  57 |   fs.writeFileSync('grades.json', JSON.stringify(grades, null, 2));
  58 | 
  59 |   expect(grades.length).toBeGreaterThan(0);
  60 | });
  61 | 
```